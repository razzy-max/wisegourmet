# Store Name Client

React + Vite frontend for Store Name.

## Local Development

1. Copy `.env.example` to `.env`.
2. Ensure `VITE_API_BASE_URL` points to the backend API.
3. Run:

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
